// Authentication configuration
// Set to false to use email/password login, true to use Google OAuth
export const useGoogleAuth = true;

// Password settings
export const passwordSettings = {
  minLength: 8,
  requireSpecialChar: true,
  requireNumber: true,
  requireUppercase: true,
};
